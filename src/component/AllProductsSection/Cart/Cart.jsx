import React from 'react';

const Cart = ({additem,setAdditem}) => {
    console.log(additem);
    return (
        <div>
            Nothing in cart add something
        </div>
    );
};

export default Cart;